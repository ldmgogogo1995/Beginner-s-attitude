function addFun(x: number, y: number): number {
  return x + y;
}
let myAdd = function (x: number, y: number): number {
  return x + y;
};
let myApp: (x: number, y: number) => number = function (
  x: number,
  y: number
): number {
  return x + y;
};
